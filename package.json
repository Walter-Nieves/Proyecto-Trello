{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy":"npm run build",
    "deploy":"gh-pages -d dist",
    "actualizar": "npx sass --watch style.scss:style.css"

  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.86.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "extract-uri-image": "^1.1.0"
  }
}
